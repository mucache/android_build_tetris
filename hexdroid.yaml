name: Tetris-Build

on:
  push:
    branches:
      - main

# This tells HexDroid what to do on its own servers
build:
  engine: aosp
  config:
    manifest_url: https://github.com/mucache/manifest
    manifest_branch: 16.2
    device: lineage_Tetris-userdebug
    command: mka bacon
