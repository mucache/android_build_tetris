name: VoltageOS Build (Cloud-Sync)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc "/usr/local/share/boost" "$AGENT_TOOLSDIRECTORY"
          sudo apt-get clean

      - name: Setup Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y bc build-essential zip curl git wget python3 rclone
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          git config --global user.email "build@example.com"
          git config --global user.name "Builder"

      - name: Setup Rclone
        env:
          RCLONE_CONF_BASE64: ${{ secrets.RCLONE_CONFIG }}
        run: |
          mkdir -p ~/.config/rclone
          echo "$RCLONE_CONF_BASE64" | base64 -d > ~/.config/rclone/rclone.conf

      - name: Restore Cache from G-Drive
        run: |
          # Restore CCACHE to speed up build
          mkdir -p ~/.ccache
          rclone copy gdrive:voltage/ccache ~/.ccache -P || echo "No ccache found."

      - name: Repo Sync (Local Fast)
        run: |
          export PATH=$PATH:~/bin
          mkdir -p ~/voltage && cd ~/voltage
          
          # We use local disk for Repo to avoid G-Drive symlink/rename errors
          # Shallow sync to fit in 80GB
          repo init -u https://github.com/VoltageOS/manifest.git -b 15 --depth=1
          
          mkdir -p .repo/local_manifests
          cat <<EOF > .repo/local_manifests/tetris.xml
          <?xml version="1.0" encoding="UTF-8"?>
          <manifest>
            <remote name="github" fetch="https://github.com/" />
            <project name="valt-x/device_nothing_tetris" path="device/nothing/Tetris" remote="github" revision="lineage-23.2" />
            <project name="valt-x/android_device_nothing_Tetris-kernel" path="device/nothing/Tetris-kernel" remote="github" revision="lineage-23.2" />
            <project name="valt-x/vendor_nothing_Tetris" path="vendor/nothing/Tetris" remote="github" revision="lineage-23.2" />
            <project name="SuperAviation001/android_hardware_mediatek" path="hardware/mediatek" remote="github" revision="lineage-22.2" />
          </manifest>
          EOF

          repo sync -c -j8 --force-sync --no-clone-bundle --no-tags --current-branch
          df -h

      - name: Build ROM
        run: |
          export PATH=$PATH:~/bin
          cd ~/voltage
          source build/envsetup.sh
          lunch voltage_Tetris-userdebug
          
          export USE_CCACHE=1
          export CCACHE_DIR=~/.ccache
          ccache -M 30G
          
          mka bacon -j$(nproc --all)

      - name: Save Cache & Upload ROM
        if: always()
        run: |
          # Save ccache back to G-Drive for next time
          rclone sync ~/.ccache gdrive:voltage/ccache -P
          
          # Upload final ROM if successful
          if [ -f ~/voltage/out/target/product/Tetris/voltage-*.zip ]; then
            cd ~/voltage/out/target/product/Tetris
            rclone copy voltage-*.zip gdrive:voltage/builds/ -P
            curl -s --upload-file voltage-*.zip https://transfer.sh/voltage-tetris.zip
          fi
